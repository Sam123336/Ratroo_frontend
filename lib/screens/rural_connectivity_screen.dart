import 'package:flutter/material.dart';

class RuralConnectivityScreen extends StatelessWidget {
  const RuralConnectivityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rural Connectivity')),
      body: const Center(child: Text('Rural Connectivity UI coming soon')),
    );
  }
}
